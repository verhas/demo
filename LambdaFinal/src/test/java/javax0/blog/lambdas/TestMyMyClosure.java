package javax0.blog.lambdas;

import groovy.lang.Closure;
import org.assertj.core.api.Assertions;
import org.junit.jupiter.api.DisplayName;
import org.junit.jupiter.api.Test;

public class TestMyMyClosure {
/*
    @Test
    @DisplayName("Incremented always increments with the given amount")
    void testIncrementer() {
        // snippet TestMyMyClosure
        Closure inc = (Closure)MyMyClosure.incrementer();
        Assertions.assertThat(inc.call(1)).isEqualTo(2);
        Assertions.assertThat(inc.call(1)).isEqualTo(3);
        Assertions.assertThat(inc.call(1)).isEqualTo(4);
        // end snippet
    }
*/
}
